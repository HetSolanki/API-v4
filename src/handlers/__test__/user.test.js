describe("User authentication", () => {
    it("SignUp", () => {
        expect(1).toBe(1);
    })
    it("SignIn", () => {
        expect(1).toBe(1);
    })
})

describe("User roles & previleges", () => {
    it("roles of the user", () => {
        expect("hello").toEqual("hello");
    })
})